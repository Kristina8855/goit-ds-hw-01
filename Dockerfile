# Використовуємо офіційний образ Python 3.8
FROM python:3.8-slim

# Встановлюємо робочу директорію /app у контейнері
WORKDIR /app

# Копіюємо файли додатку у контейнер
COPY . /app

# Встановлюємо необхідні залежності
RUN pip install --no-cache-dir -r requirements.txt

# Вказуємо команду для запуску застосунку "Персональний помічник" при старті контейнера
CMD ["python", "main.py"]
